{
  "name": "domains-registry",
  "version": "1.0.0",
  "description": "Hrv Clan Domain Registration System",
  "main": "index.js",
  "scripts": {
    "test": "npx ava tests/*.test.js --verbose"
  },
  "devDependencies": {
    "ava": "^5.0.0"
  },
  "dependencies": {
    "fs-extra": "^11.0.0"
  }
}
